[package]
name = "donation_registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "21.5.0", default-features = false }

[dev-dependencies]
soroban-sdk = { version = "21.5.0", default-features = false, features = ["testutils"] }
